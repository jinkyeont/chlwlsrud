#include "1.h"
#include <stdio.h>

int main(int argc, char **argv)
{
        int a = 100, b = 25;

        printf("add : %d\n", add(a,b));
        printf("sub : %d\n", sub(a,b));
        printf("mul : %d\n", mul(a,b));
        printf("div : %d\n", div(a,b));
}

